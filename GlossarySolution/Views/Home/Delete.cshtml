@model GlossarySolution.Models.DefinitionModel

@{
    ViewBag.Title = "Delete";
}

<h1 class="sub-title">Delete a Term Definition</h1>

<div class="alert alert-warning" style="display: flex;">
    <div style="font-size: 1.4em;padding-right: 20px;"><i class="fas fa-exclamation-circle"></i></div>
    <div style="padding-top:5px;">Are you sure you want to delete this?</div>
</div>
<div>
    <hr />
    <div class="form-horizontal">
        <div class="form-group">
            <label class="control-label">
                @Html.DisplayNameFor(model => model.Term)
            </label>

            <div>
                @Html.DisplayFor(model => model.Term)
            </div>
        </div>
        <div class="form-group">
            <label class="control-label">
                @Html.DisplayNameFor(model => model.TermDefinition)
            </label>

            <div>
                @Html.DisplayFor(model => model.TermDefinition)
            </div>
        </div>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        @*<div class="form-row form-row-buttons">
            <div class="col-md-8">
                <a href="@Url.Action("Edit", "Home", new { id = Model.DefinitionId })" class="btn btn-default">Edit</a>
            </div>
            <div class="col-md-3" style="right: 0px;">
                <a href="@Url.Action("List", "Home", new { id = Model.DefinitionId })" class="btn btn-light">Index</a>
            </div>
        </div>*@

    <div class="form-actions no-color form-row">
        <div class="col-md-2">
            <input type="submit" value="Delete" class="btn btn-outline-danger btn-block btn-sm" style="font-weight: 500" />
        </div>
        <div class="col-md-2">
            <a href="@Url.Action("Edit", "Home", new { id = Model.DefinitionId })" class="btn btn-light btn-block btn-sm">Edit</a>
        </div>
        <div class="col-md-2">
            <a href="@Url.Action("Index", "Home")" class="btn btn-light btn-block btn-sm">Index</a>
        </div>
    </div>
    }
</div>
